Question
========

Beim R Aufruf

```{r, eval = FALSE}
str(Meine.Daten)
```

Was ist hier die *Funktion*?

Answerlist
----------
* `str()`
* `Meine.Daten`
* Es gibt keine Funktion.

Solution
========

Answerlist
----------
* Richtig. Die Funktion `str()` gibt die *Struktur* eines Objektes in R zurück. Hier die Struktur des Datensatzes `Meine.Daten`.
* Falsch. Die Funktion `str()` gibt die *Struktur* eines Objektes in R zurück. Hier die Struktur des Datensatzes `Meine.Daten`.
* Falsch. Die Funktion `str()` gibt die *Struktur* eines Objektes in R zurück. Hier die Struktur des Datensatzes `Meine.Daten`.

Meta-information
================
exname: Funktion
extype: schoice
exsolution: 100