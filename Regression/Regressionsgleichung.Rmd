```{r data generation, echo = FALSE, results = "hide"}
library(mosaic)
data(mtcars)
coefs <- lm(mpg ~ wt, data = mtcars) %>%
  coef() %>%
  round()
```

Question
========

Eine lineare Regression des Verbrauchs `mpg` auf das Gewicht `wt` des Datensatzes `mtcars` ergibt:

```{r}
erglm <- lm(mpg ~ wt, data = mtcars)
erglm
```

Wie lautet die geschätzte Regressionsgleichung (gerundet)?

Answerlist
----------
* $\hat{\text{mpg}} = `r coefs[1]`  `r coefs[2]` \cdot \text{wt}$
* $\hat{\text{mpg}} = `r coefs[2]` + `r coefs[1]` \cdot \text{wt}$
* $\hat{\text{wt}} = `r coefs[1]`  `r coefs[2]` \cdot \text{mpg}$
* $\hat{\text{wt}} = `r coefs[2]` + `r coefs[1]` \cdot \text{mpg}$



Solution
========

Answerlist
----------

* Richtig
* Falsch
* Falsch
* Falsch

Meta-information
================
exname: Regressionsgleichung
extype: schoice
exsolution: 1000