```{r data generation, echo = FALSE, results = "hide"}
library(mosaic)
data(mtcars)
r2 <- lm(mpg ~ wt, data = mtcars) %>%
  rsquared() %>%
  round(.,3)
```

Question
========

Das Bestimmtheitsmaß $R^2$ (`rsquared()`) einer linearen Regression der Reichweite `mpg` auf das Gewicht `wt` des Datensatzes `mtcars` ergibt:

```{r}
erglm <- lm(mpg ~ wt, data = mtcars)
rsquared(erglm)
```

Welche Interpretation ist richtig?

Answerlist
----------
* $`r r2*100`\,\%$ der Beobachtungen werden richtig modelliert.
* $`r r2*100`\,\%$ der Variation von `mpg` kann linear durch `wt` modelliert werden.
* Die Wahrscheinlichkeit, dass das lineare Modell stimmt, liegt bei $`r r2*100`\,\%$.




Solution
========

Answerlist
----------

* Falsch
* Richtig
* Falsch

Meta-information
================
exname: Interpretation R-Quadrat
extype: schoice
exsolution: 010